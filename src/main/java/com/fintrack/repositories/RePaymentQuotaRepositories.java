package com.fintrack.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import com.fintrack.model.RePaymentQuota;

public interface RePaymentQuotaRepositories extends JpaRepository<RePaymentQuota, Integer> {

}
